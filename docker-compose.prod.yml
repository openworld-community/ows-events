version: "3.7"

x-logging: &default-logging
  driver: json-file
  options:
    max-size: '10m'
    max-file: '5'

services:
  frontend:
    build:
      context: ./
      dockerfile: Dockerfile
    logging: *default-logging
    restart: always
    ports:
      - "7080:7080"

