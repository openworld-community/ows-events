export type IEventSitemapData = {
	id: string;
	updatedAt: Date;
};

export type ICountrySitemapData = {
	country: string;
	updatedAt: Date;
};

export type ICitySitemapData = {
	city: string;
	updatedAt: Date;
};
